import React from 'react';
import './home.css';

const Home = () => {
    return (
        <div id='home-box' className='' style={{minHeight: '100vh'}}>
            <div id='home-content' style={{minHeight: '100vh'}}>
                
            </div>

        </div>
    )
}

export default Home;